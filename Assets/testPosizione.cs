using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testPosizione : MonoBehaviour
{
    //GameObject LeftHand;
   // ChainIKConstraint ck;
    // Start is called before the first frame update
    void Start()
    {
       
       // LeftHand= GameObject.FindGameObjectsWithTag("LeftHand");
        
         // ck.target=  LeftHand.transform.GetChild(0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
